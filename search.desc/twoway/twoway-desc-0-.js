searchState.loadedDescShard("twoway", 0, "<strong>This crate is deprecated. Use crate <code>memchr</code> instead.</strong>\n<code>find_bytes</code> finds the first ocurrence of <code>pattern</code> in the <code>text</code>…\n<code>find_str</code> finds the first ocurrence of <code>pattern</code> in the <code>text</code>.\n<code>rfind_bytes</code> finds the last ocurrence of <code>pattern</code> in the <code>text</code>…\n<code>rfind_str</code> finds the last ocurrence of <code>pattern</code> in the <code>text</code> …")